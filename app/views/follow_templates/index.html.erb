<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All follows
    </h1>

    <a href="/follows/new" class="btn btn-block btn-outline-secondary">
      Add a new follow
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Status
        </th>

        <th>
          Recipient
        </th>

        <th>
          Sender
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @follows.each do |follow| %>
      <tr>
        <!-- Display follow.id start -->
        <td>
          <%= follow.id %>
        </td>
        <!-- Display follow.id end -->

        <!-- Display follow.status start -->
        <td>
          <%= follow.status %>
        </td>
        <!-- Display follow.status end -->

        <!-- Display follow.recipient_id start -->
        <td>
          <% if follow.receiver.present? %>
            <a href="/users/<%= follow.receiver.id %>">
              <%= follow.receiver.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display follow.recipient_id end -->

        <!-- Display follow.sender_id start -->
        <td>
          <% if follow.sender.present? %>
            <a href="/users/<%= follow.sender.id %>">
              <%= follow.sender.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display follow.sender_id end -->

        <!-- Display follow.created_at start -->
        <td>
          <%= time_ago_in_words(follow.created_at) %> ago
        </td>
        <!-- Display follow.created_at end -->

        <!-- Display follow.updated_at start -->
        <td>
          <%= time_ago_in_words(follow.updated_at) %> ago
        </td>
        <!-- Display follow.updated_at end -->

        <td>
          <a href="/follows/<%= follow.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @follows, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
